import 'package:flutter/material.dart';

class DocumentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('หน้าจัดเก็บเอกสาร', style: TextStyle(fontSize: 24)),
    );
  }
}
